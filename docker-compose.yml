services:
  postgres:
    image: postgres:16
    container_name: bcbs-postgres
    environment:
      POSTGRES_DB: bcbs
      POSTGRES_USER: bcbs_user
      POSTGRES_PASSWORD: bcbs_password
    ports:
      - "5433:5432"
    volumes:
      - bcbs-postgres-data:/var/lib/postgresql/data

volumes:
  bcbs-postgres-data: